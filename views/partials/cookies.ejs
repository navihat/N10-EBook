<!DOCTYPE html>
<html lang="vi">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Cookie Notification</title>
  <style>
    * {
      box-sizing: border-box;
      margin: 0;
      padding: 0;
    }

    body {
      font-family: Arial, sans-serif;
      background: #76aee0;
      color: #1d3557;
      display: flex;
      justify-content: center;
      align-items: center;
      height: 100vh;
      text-align: center;
      position: relative;
      overflow: hidden;
    }

    .container {
      max-width: 600px;
      padding: 40px;
      position: relative;
      z-index: 1;
    }

    h1 {
      font-size: 32px;
      font-weight: bold;
      margin-bottom: 40px;
      line-height: 1.4;
    }

    .btn {
      display: inline-block;
      padding: 12px 24px;
      margin-top: 20px;
      font-size: 18px;
      color: #1d3557;
      border: 2px solid #1d3557;
      background: transparent;
      text-decoration: none;
      border-radius: 8px;
      position: relative;
      transition: all 0.3s ease;
    }

    .btn:hover {
      background-color: #1d3557;
      color: #fff;
    }

    .arrow {
      font-size: 40px;
      margin-top: 20px;
      display: block;
    }

    .footer-text {
      margin-top: 30px;
      font-size: 16px;
      color: #1d3557;
    }

    .bg-shape {
      position: absolute;
      width: 200px;
      height: 200px;
      background: linear-gradient(145deg, #4582c6, #73b0ec);
      border-radius: 30px;
      transform: rotate(45deg);
      opacity: 0.6;
      z-index: 0;
    }

    .shape1 {
      top: -80px;
      left: -80px;
    }

    .shape2 {
      bottom: -80px;
      right: -80px;
    }

    /* Cookie Banner */
    #cookie-banner {
      position: fixed;
      bottom: 20px;
      left: 50%;
      transform: translateX(-50%);
      background: #ffffffee;
      border: 1px solid #ccc;
      padding: 20px;
      border-radius: 12px;
      box-shadow: 0 4px 8px rgba(0,0,0,0.2);
      z-index: 999;
      display: none;
    }

    #cookie-banner p {
      margin-bottom: 10px;
      font-size: 14px;
      color: #333;
    }

    #cookie-banner .cookie-buttons {
      display: flex;
      gap: 10px;
      justify-content: center;
    }

    .cookie-btn {
      padding: 8px 16px;
      border: none;
      border-radius: 6px;
      cursor: pointer;
      font-weight: bold;
    }

    .accept {
      background-color: #2ecc71;
      color: white;
    }

    .decline {
      background-color: #e74c3c;
      color: white;
    }
  </style>
</head>
<body>
  <div class="bg-shape shape1"></div>
  <div class="bg-shape shape2"></div>

  <div class="container">
    <h1>HOW TO BUILD AN<br> AUDIENCE AROUND A<br> NICHE TOPIC</h1>
    <a href="#" class="btn">LEARN MORE</a>
    <span class="arrow">&#8594;</span>
    <div class="footer-text">reallygreatesite.com</div>
  </div>

  <!-- Cookie Banner -->
  <div id="cookie-banner">
    <p>Trang web này sử dụng cookie để cải thiện trải nghiệm người dùng. Bạn có chấp nhận không?</p>
    <div class="cookie-buttons">
      <button class="cookie-btn accept" onclick="acceptCookies()">Chấp nhận</button>
      <button class="cookie-btn decline" onclick="declineCookies()">Từ chối</button>
    </div>
  </div>

  <script>
    // Kiểm tra trạng thái cookie
    window.onload = function() {
      if (!localStorage.getItem('cookieConsent')) {
        document.getElementById('cookie-banner').style.display = 'block';
      }
    };

    function acceptCookies() {
      localStorage.setItem('cookieConsent', 'accepted');
      document.getElementById('cookie-banner').style.display = 'none';
    }

    function declineCookies() {
      localStorage.setItem('cookieConsent', 'declined');
      document.getElementById('cookie-banner').style.display = 'none';
    }
  </script>
</body>
</html>
